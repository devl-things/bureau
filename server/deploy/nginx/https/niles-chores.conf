# Chores - subdomain routing
# Host: chores.devl.lan
# Web:  /
# API:  /api/

server {
    listen 80;
    server_name ${DEPLOY__NGINX__CHORES__SERVER_NAME};

    # LAN-only access
    include /etc/nginx/snippets/lan_only.conf;

    location ^~ /api/ {
        proxy_pass ${DEPLOY__NGINX__CHORES__API_UPSTREAM};
        include /etc/nginx/snippets/proxy_common.conf;
    }

    location / {
        proxy_pass ${DEPLOY__NGINX__CHORES__WEB_UPSTREAM};
        include /etc/nginx/snippets/proxy_common.conf;
    }
}