@using Sven.PageModels.Components;
@model UserNavViewModel

@if (Model.IsAuthenticated)
{
    <ul class="navbar-nav ms-auto">
        <li class="nav-item">
            <span class="navbar-text me-3">@Model.Email</span>
        </li>
        <li class="nav-item">
            <a class="btn btn-outline-danger" href="/connect/signout">Sign out</a>
        </li>
    </ul>
}